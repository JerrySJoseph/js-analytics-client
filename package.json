{
  "name": "js-analytics-client",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/analytics.js",
  "scripts": {
    "dev": "nodemon",
    "build": "npx parcel build src/index.js --detailed-report --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.7",
    "parcel": "^2.12.0"
  }
}